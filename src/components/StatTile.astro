---
interface Props {
  label: string;
  value: string;
  hint?: string;
  tone?: "default" | "go" | "investigate" | "reject";
  featured?: boolean;
}
const { label, value, hint, tone = "default", featured = false } = Astro.props;
---

<div class:list={["stat", `stat--${tone}`, { "stat--featured": featured }]}>
  <p class="stat__label">{label}</p>
  <p class="stat__value">{value}</p>
  {hint && <p class="stat__hint">{hint}</p>}
</div>

<style>
  .stat {
    background: var(--bg-surface);
    border: 1px solid var(--border-subtle);
    border-radius: var(--radius-lg);
    padding: var(--space-5);
  }
  .stat--featured {
    border-color: color-mix(in srgb, var(--gold-500) 40%, var(--border-subtle));
    box-shadow: inset 0 0 0 1px color-mix(in srgb, var(--gold-500) 12%, transparent);
  }
  .stat--featured .stat__label {
    color: var(--gold-400);
  }
  .stat__label {
    margin: 0 0 var(--space-2);
    font-size: var(--text-sm);
    color: var(--text-muted);
  }
  .stat__value {
    margin: 0;
    font-size: var(--text-2xl);
    font-weight: var(--weight-semibold);
    color: var(--text-primary);
    line-height: 1.1;
  }
  .stat__hint {
    margin: var(--space-2) 0 0;
    font-size: var(--text-xs);
    color: var(--text-muted);
  }
  .stat--go .stat__value {
    color: var(--status-go);
  }
  .stat--investigate .stat__value {
    color: var(--status-investigate);
  }
  .stat--reject .stat__value {
    color: var(--status-reject);
  }
</style>
